# Ai generated problem

# 1. Create a list with 3 fruits
# 2. Print the list
# 3. Create a list with numbers: 1, 2, 3, 4
# 4. Print the length of that list
# 5. Create a list with mixed data types:
# string, integer, boolean
# 6. Print the type of the list
# 7. Create a list using list() constructor
# 8. Create a list that has duplicate values
# 9. Print that list
# 10. Explain (comment):
# why lists are called "changeable"

fruits = ["Mango", "Lemon", "Banana"]

print(fruits)

numbers = [1, 2, 3, 4]

print(len(numbers))

mixed = ["Wazzup", 99, False]

print(type(mixed))

listConstructor = list(("Monitor", "Keyboard", "Mouse"))
print(listConstructor)

duplicate = ["Wawang", "Insecticide", "Aerosol", "Wawang"]

print(duplicate)

# because we can change, add, and remove items in a list after we created it
