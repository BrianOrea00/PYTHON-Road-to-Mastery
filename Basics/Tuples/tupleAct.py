# AI generated problem

# 1. Create a tuple with 3 fruits
# 2. Print the tuple
# 3. Print the first item
# 4. Print the last item
# 5. Print length of tuple
# 6. Create a tuple with duplicate value
# 7. Create a tuple with mixed data types
# 8. Create a tuple with ONE item (important)
# 9. Explain (comment):
# why tuple is unchangeable
# 10. Explain (comment):
# difference between list and tuple

fruits = ("lime", "lemon", "calamasi")

print(fruits)

print(fruits[0])

print(fruits[-1])

print(len(fruits))

dupTuple = ("ls", "cd", "sudo rm -rf /", "sudo rm -rf /")
print(dupTuple)

mixTuple = ("qwer", 1, True)
print(mixTuple)

oneTuple = ("This is One Tuple",)
print(type(oneTuple))
print(oneTuple)

# tuples are unchangeable because they are designed to be immutable (cannot modify after creation)

# the difference is that a list is mutable (we can change it) while a tuple is immutable (we can't)
